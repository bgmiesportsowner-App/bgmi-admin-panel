// src/pages/Settings.jsx
const Settings = () => {
  return (
    <div className="page">
      <h2 className="page-title">Settings</h2>
      <p className="page-subtitle">
        Future: Tournament rules, entry fees, payout settings etc.
      </p>

      <div className="settings-box">
        <p>For now, this is a placeholder page.</p>
        <p>You can add more admin settings later.</p>
      </div>
    </div>
  );
};

export default Settings;
